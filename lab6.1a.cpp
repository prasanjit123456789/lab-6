//to ask the user the unit price, units and taxrate and to return it
//include library
#include<iostream>
using namespace std;
//define the unit cost,units,taxrate in a functin
float totalCstWithTax(float &p, int &q,float &r){return 0;}
//define main function
int main(){
	//define variables
	float c,t; int u; 
	//ask the user about the unit cost, units,taxrate
	cout<<"Give the values of unit cost, units, taxrate."<<endl;
	cin>>c>>u>>t;
	//argue with the totalCstWithTax
	totalCstWithTax(c,u,t);
}
